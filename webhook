const url = '---MAKE_WEBHOOK_URL';
const body = {
  fullName: workflow.fullName,
  email: workflow.email,
  phoneNum: workflow.phoneNumber,
  bookingReason: workflow.bookingReason
};

await axios.post(url, body);
